Copy these directories into the directory that the Salt master configuration
parameter `extension_modules` points to. See
https://docs.saltstack.com/en/latest/ref/configuration/master.html#extension-modules.
